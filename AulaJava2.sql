CREATE TABLE 
	Classe(
		NumeroClasse Int,
		NomeClasse varchar(100),
		 PRIMARY KEY (NumeroClasse)
		);

		SELECT *FROM Classe

		CREATE TABLE 
	Disciplina(
		Id Int,
		Nome varchar(100),
		 PRIMARY KEY (Id)
		);
				SELECT *FROM Disciplina


		CREATE TABLE 
	Professor(
		Id Int,
		Nome varchar(100),
		 PRIMARY KEY (ID)
		);
		SELECT *FROM Professor

